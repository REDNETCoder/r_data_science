This folder contains files that analyze gun murder data from the US collected by the FBI.

download-data.r - downloads csv file that contains the data to the "data" directory

wrangle-data.r - creates a dataset from the csv file and saves as R Object in the "rda" directory

analysis.r - generates a barplot and saves it to the "figs" directory